Todos:
* "'hello world' \"hi all\" 1024 true null 3.14 =" should not success. Nees semi-comma(;) operator.
* Change vector library
* Removing the newline will operate on each statement execution.
    for (var i = 0;
    i < 5; ++i)
        console.log(
        i
        );

* brama should handle following codes
- this.data
- this.data[this.getIndex()]
- this.self.data.content
* add tests for continue
* use dmalloc on mac and linux for memory leak
* accessor configure for assignment
* constants, variables, temporaries should be in same list.